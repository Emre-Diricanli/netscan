package net

// package netutil

// Placeholder: we'll add mDNS browse/cache later if you want it.
// For now we rely on ReverseLookup (PTR) in hosts.go.
// Keeping this file so imports remain stable.

func MDNSLookup(ip string) string {
	return ""
}
